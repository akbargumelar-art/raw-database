# Database Configuration
DB_HOST=localhost
DB_USER=rawdata_user
DB_PASSWORD=GANTI_DENGAN_PASSWORD_ANDA
DB_NAME=dataflow_pro
DB_PORT=3306

# JWT Secret - Generate dengan: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GANTI_DENGAN_RANDOM_STRING_YANG_KUAT

# Server Port
PORT=5003

# Node Environment
NODE_ENV=production
